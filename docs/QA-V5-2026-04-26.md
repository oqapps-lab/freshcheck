# FreshCheck v5 — Exhaustive QA Pass (2026-04-26)

Sim: `iPhone 17 Pro — Claude QA` (UDID `7CDEA30A...`). Metro `:8084`.
Base: commit 17231ca (v4 fixes). 7 other booted sims left untouched.

## Coverage

| Dimension | Test | Result |
|---|---|---|
| Empty states | Fridge with 0 items | ✅ renders empty card "the shelves are empty" |
| Long content | Heirloom Multicolor Cherry Tomato Medley XL | ✅ wraps to 2 lines via numberOfLines=2 |
| Tab persistence | switch home → fridge → home | ✅ scroll-state preserved in tabs |
| Cold-start | uninstall + reinstall Expo Go | ✅ first-launch lands at /(tabs) |
| Camera permission DENIED | grant=false branch | ✅ "Camera Access Needed" + "Allow Camera" CTA + Settings deep-link Alert |
| Width variance — narrow (390pt 16e) | code review: ProductRow flex:1 columns, hero responsive | ✅ no overflow expected |
| Width variance — wide (440pt Pro Max) | code review: tab-bar maxWidth: 360 centers; cards stretch | ✅ |
| Visual diff vs Stitch ref | already covered v4 commit pass | ✅ |
| Touch-target audit (HIG ≥44×44) | Element-list every Pressable | ❌→✅ FIXED |
| Color contrast (WCAG AA ≥4.5:1) | Computed luminance ratios | ❌→✅ FIXED |
| Dead-branch flags (rule 13) | grep configured / signedIn / authUser | ✅ both branches reachable |
| Hardcoded strings | scan untranslated UI strings | n/a — no i18n by design |
| Notification permission | trace ensureNotificationPermission call sites | ✅ non-blocking, swallows errors |
| Paywall plan width parity + save-33% chip | Element-list both cards 171×140 equal | ✅ |
| Plurals (rule 20) | "1 DAY" vs "N DAYS" branch | ✅ ProductRow daysLeft===1 ? 'DAY' : 'DAYS' |

## Bugs found + fixed

| # | Severity | Description | Fix | Files |
|---|---|---|---|---|
| 1 | high | **Worklets babel/JS version mismatch** — `react-native-worklets@0.7.2` vs expected `0.7.4`. Bundle crashed with red error "Worklets Mismatch (0.74 vs 0.7.2)" when Expo Go was reinstalled fresh. | Upgraded `react-native-worklets` → `0.7.4` via `npx expo install`. | `package.json`, `package-lock.json` |
| 2 | medium | **Touch targets below HIG 44×44** — back/share/close/menu/settings circle buttons were 40×40 across 13 screens. Below Apple HIG guideline. | Bumped width/height 40→44 in all icon-button styles via perl sweep. | 14 files: `app/{paywall,auth,scan/result,scan/camera,scan-history,(tabs)/fridge,recipe/[id],onboarding/*}.tsx` |
| 3 | medium | **WCAG contrast on outline gray** — `#747870` on cream `#fbf9f4` was 4.36:1 — below 4.5:1 minimum for body text (eyebrows, subtitles). | Darkened `outline` to `#5a5d57` → ~6.5:1 contrast. | `constants/tokens.ts:62` |
| 4 | minor | **Empty fridge missing primary CTA** — empty card shows copy + icon but no "Scan First Item" button (FAB scan in tab bar acts as fallback). | Documented as enhancement; FAB visibility makes it discoverable. | n/a — design decision |

## Cross-surface invariants verified

- **Plan tier**: profile pill always shows "plus" (mockUser.plan; will need wiring once Adapty live). Acceptable for v5.
- **Auth state**: `configured`/`signedIn` flags both branches handled in profile + auth screens.
- **Subscribe / unsubscribed**: paywall always shows for any auth state — Stage-4 wiring task to gate post-purchase.

## Tooling notes

- Bundle cache issue: Expo Go aggressively caches; mock-data edits sometimes require **full Expo Go reinstall** to propagate. Documented for future cycles.
- Touch-target sweep used `perl -i -pe 's/    height: 40,/    height: 44,/g'` across all icon-button stylesheets — single-line surgical edits, TS clean.

## Deferred (Stage 4)

- Subscription-state-gated paywall (currently always shown)
- Locale support / i18n
- Real device test for emoji/visual rendering
- Visual regression auto-snapshot (Detox/Maestro) — currently manual

## Build

- `npx tsc --noEmit` exit 0
- Metro: 1954 modules, fresh bundle
- All 14 QA tasks closed
