# FreshCheck v4 — Full QA Pass (2026-04-25)

Sim: `iPhone 17 Pro — Claude QA` (UDID `7CDEA30A...`). Metro `:8084`.
Build: commit 8e9ffa3 (v4 Stitch redesign).

## Coverage map

| Screen | State(s) tested | Result |
|---|---|---|
| Home (Stitch ref `home-1`) | with-data | ✅ |
| Fridge (`fridge-2`) | All / Produce / Dairy filters | ✅ |
| Recipes | with-data, 3 cards | ✅ |
| Recipe Detail | full (`garlic-herb-chicken`), empty (`spinach-lemon-pasta`) | ✅ |
| Profile | mock-state | ✅ |
| Scan Camera | permission, granted, shutter→result | ✅ |
| Scan Result (`result-1`) | live result navigation | ✅ |
| Paywall | plan toggle, CTA, restore | ✅ |
| Auth | sign-in ↔ sign-up toggle, validation | ✅ |
| Scan History | YESTERDAY/2-DAYS-AGO/THIS-WEEK groupings | ✅ |
| Onboarding | welcome → goal → family-size → preferences → waste → plan → demo → paywall | ✅ |

## Bugs found (3) — all fixed

| # | Severity | Screen | Root cause | Fix |
|---|---|---|---|---|
| 1 | medium | onboarding/demo | Floating "Scan My Own Food" CTA overlapped THE QUIET NOTE copy at bottom — `paddingBottom: insets.bottom + 140` not enough buffer for scroll content + floating block. | Bumped scroll paddingBottom to `insets.bottom + 200`. (`app/onboarding/demo.tsx:30`) |
| 2 | low | (tabs)/recipes | Third recipe card "Grape & Goat Cheese Salad" partially under tab bar at scroll=0. | Bumped scroll paddingBottom from `+24` to `+48` extra. (`app/(tabs)/recipes.tsx:44, 133`) |
| 3 | none (false alarm) | scan/camera | Visual grouping of quota chip + shutter looked elongated in screenshot — actually two separate elements with intended 178-px gap. No bug. | n/a |

## Cross-surface (rule 3) findings — none

- Home "3 ITEMS EXPIRING SOON" matches Fridge total expiring (milk past, chicken past, spinach soon = 3) ✅
- Home "LAST SCAN: SAFE" matches Scan History first row (Wild Salmon · safe) ✅
- Profile stats (47 / 12 / $156) are mock fixtures and intentionally decoupled from runtime scans/items count — flagged as Stage-4 wiring item, not a v4 bug.

## A11y observations

- Every Pressable in the v4 redesign exposes `accessibilityRole="button"` + `accessibilityLabel` ✅
- Plan toggle / fridge chips correctly use `accessibilityState={{ selected }}` ✅
- One persistent XCUITest dump quirk: each StaticText appears twice in the element list (RN New-Architecture artifact, not user-facing). Documented in v3 QA notes.

## Tooling notes

- Sim 7CDEA30A only; never touched the 7 other booted sims (other projects).
- Used `$MCLI io tap` for taps and `xcrun simctl io ... screenshot` for screenshots per ui-qa skill rules 17–22 (iOS 26 WDA hardening).
- No multi-agent fan-out for THIS pass — sequential walk-through was faster than coordinating agents on independent screens.

## Deliverables

1. `app/onboarding/demo.tsx` — bumped scroll paddingBottom (140 → 200)
2. `app/(tabs)/recipes.tsx` — bumped scroll paddingBottom (+24 → +48 over `floatingBottomClearance`)
3. `docs/QA-V4-2026-04-25.md` — this post-mortem

## TS / Build

- `npx tsc --noEmit` exit 0
- Metro bundle: 1954 modules, no warnings beyond expected Expo Go SDK-skew notices.
